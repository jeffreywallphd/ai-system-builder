import type { ArtifactFamily } from "../artifact";

/**
 * Website HTML captures default to structured-text because HTML is markup, not plain text.
 */
export const WEBSITE_HTML_ARTIFACT_FAMILY: ArtifactFamily = "structured-text";
