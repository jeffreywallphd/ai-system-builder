import type { RuntimeTaskStatus } from "./runtime-task-status";

/**
 * Python runtime task status remains as a compatibility alias of RuntimeTaskStatus.
 */
export type PythonRuntimeTaskStatus = RuntimeTaskStatus;
