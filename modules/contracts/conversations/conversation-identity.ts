import { normalizeExecutionOperationId } from '../execution-runs';
export type ConversationSessionId=string&{readonly __conversationSessionIdBrand: unique symbol}; export type ConversationTurnId=string&{readonly __conversationTurnIdBrand: unique symbol}; export type ConversationMessageId=string&{readonly __conversationMessageIdBrand: unique symbol}; export type AssistantResponseId=string&{readonly __assistantResponseIdBrand: unique symbol}; export type ConversationHistoryReferenceId=string&{readonly __conversationHistoryReferenceIdBrand: unique symbol}; export type ConversationOperationId=ReturnType<typeof normalizeExecutionOperationId>;
const p=/^[a-z0-9](?:[a-z0-9._-]{0,126}[a-z0-9])?$/; const bad=/(https?:\/\/|\.\.|[\\/$]|token|secret|api[-_]?key|private[-_]?key)/i; const n=<T extends string>(v:string,l:string)=>{const t=v.trim(); if(!t||!p.test(t)||bad.test(t)) throw new Error(`${l} must be a safe non-empty identifier.`); return t as T;};
export const normalizeConversationSessionId=(v:string)=>n<ConversationSessionId>(v,'ConversationSessionId'); export const normalizeConversationTurnId=(v:string)=>n<ConversationTurnId>(v,'ConversationTurnId'); export const normalizeConversationMessageId=(v:string)=>n<ConversationMessageId>(v,'ConversationMessageId'); export const normalizeAssistantResponseId=(v:string)=>n<AssistantResponseId>(v,'AssistantResponseId'); export const normalizeConversationHistoryReferenceId=(v:string)=>n<ConversationHistoryReferenceId>(v,'ConversationHistoryReferenceId');
