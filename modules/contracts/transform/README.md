# Transform Contracts

Transform contracts capture ELT-style derivation semantics without binding to specific engines.

This family provides:

- transform kind and stage vocabularies
- explicit transform identity references (`definitionId` and optional `executionId`)
- normalized transform specifications (definition identity)
- transform execution records and summary envelopes (execution identity)
